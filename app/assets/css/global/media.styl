// TODO: Add variants

phoneWidth   = 748px
tabletWidth  = 1024px
tabletWidthP = 768px

mqPhone    = "only screen and (max-width: " + phoneWidth + ")"
mqTablet   = "only screen and (max-width: " + tabletWidth + ")"
mqTabletP  = "only screen and (max-width: " + tabletWidthP + ")"
